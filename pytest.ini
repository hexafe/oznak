[pytest]
pythonpath =
    .
    src
testpaths = tests
markers =
    cli_integration: CLI integration tests for command parsing, output artifacts, and failure handling
    integration: Opt-in live database integration tests; excluded from default release test gate
